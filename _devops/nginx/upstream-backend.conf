# Active upstream — managed by deploy.sh --blue-green
# This file is included by nginx.conf and overwritten during blue-green deployments.
# Default: standard backend service name.
server backend:8000;
